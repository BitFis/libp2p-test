package server

/**
 * Simplistic rest-server, which enables printing internal state of
 * the libp2p dht. Printing the list of connected end clients
 * and other bootstrap-servers.
 *
 * This should enable better monitoring of the state of the libp2p
 * network.
 */

func SomeFunc() string {
	if true {
		return "Hey"
	}

	return "Hello World"
}
